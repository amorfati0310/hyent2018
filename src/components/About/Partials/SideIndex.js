import React, { Fragment } from 'react';
import styled from 'styled-components';


// TODO: Create Side Index Component

const SideIndex = ({ label }) => {
  return (
    <Fragment>

    </Fragment>
  );
};

export default SideIndex;
