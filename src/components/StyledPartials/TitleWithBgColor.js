import styled from 'styled-components';

export const Title = styled.div`
  width: 182px;
  height: 70px;
  background: url(${props => props.titleImage}) no-repeat center;
  background-size: contain;
`;
