import React, { Fragment, PureComponent } from 'react';

// TODO: Import Each Menu Text Images

class SideMenu extends PureComponent {
  render() {
    return (
      <Fragment>

      </Fragment>
    );
  }
}

export default SideMenu;
